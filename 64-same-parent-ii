public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if (con.AccountId != null && opp.AccountId != null && con.AccountId == opp.AccountId){
        return con.AccountId == acc.Id ? true : false;
    }
    return false;
}