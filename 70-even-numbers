public List<Integer> evenNumbers(Integer n) {
    //code here
    List<Integer> intList = new List<Integer>();
    for(Integer i=0; i<n ;i++){
        intList.add((i+1)*2);
    }
    return intList;
}