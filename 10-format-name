public String formatName(String firstName, String lastName) {
    //code here
    return lastName == null || lastName == '' ? firstName : firstName == null || firstName == '' ? lastName : lastName+', '+firstName;
}