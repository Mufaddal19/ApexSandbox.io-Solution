public String formatName(String firstName, String lastName) {
    //code here
    return firstName == null || firstName == '' ? lastName : lastName == null || lastName == '' ? firstName : lastName+', '+firstName;
}