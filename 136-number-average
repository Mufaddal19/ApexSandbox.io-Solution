public Decimal average(List<Decimal> numbers){
    // your code here
    Integer sum = 0;
    Integer totalCount = numbers.size();
    for(Integer num : numbers){
        sum += num;
    }
    Integer avg = sum/totalCount;
    return avg.setscale(2);
}