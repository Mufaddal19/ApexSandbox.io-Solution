public static Integer findLargest(List<Integer> nums) {
	//code here
    Integer largest = null;
    for(Integer i=0; i < nums.size(); i++){
        if(Integer.valueOf(nums[i]) > Integer.valueOf(largest)){
            largest = Integer.valueOf(nums[i]);
        }
    }
    return largest;
}